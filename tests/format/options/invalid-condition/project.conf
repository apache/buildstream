name: test

options:
  opt:
    type: bool
    description: Whether to build in an opt prefix
    default: False

variables:
  (?):
  - not: Allowed any adjacent keys beside the expression.
    opt:
      prefix: "/opt"
